import * as React from 'react';
import asWorkspace from "../../../components/Workspace";

class Workspace extends React.Component<{}, {}, any> {
    render() {
        return (
            <div>
            </div>
        );
    }
}

export default asWorkspace(Workspace);
